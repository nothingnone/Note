```python
# normal process
import matplotlib.pyplot as plt
f = plt.figure()
ax = f.add_subplot(m,n,index)
ax.plot(X, Y, color= "blue", alpha = 0.5, marker = 'x', linestyle = 'dashed'/[], linewidth = 2, markersize = 3)
ax.add_patch(plt.Rectangle([x,y], w, h, color, alpha))
ax.set_xlim([low, up])
ax.imshow(Matrix)
ax.clear()
figure.show()
```
