```python
tf.nn.conv2d(input, filter, strides, paddings, \
use_cudnn_on, data_format, dilations, name, filters)
# data_format:"NHWC"/"CHWN".N:batch num, H:height, W:width C:channel.
# input: tensor as data_format.
# filter: 
# strides: 
```